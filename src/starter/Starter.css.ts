import { css } from "lit";

export const styles = css`
    .tab-icons {
        margin-left: 20px; /* Push icons to the right end */
        display: flex;
        margin-top: 30px;
        margin-right:20px;
        gap: 40px;
    }

    .tab-action-icon {
        width: 20px;
        height: 20px;
        cursor: pointer;
    }
    .tab-logo img {
        height: 70px;
        width: auto;
    }
`